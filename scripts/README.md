# Legacy scripts/

**Canonical scripts live in `/_scripts/`** (see `AGENTS.md`).

This folder keeps older helpers; prefer:

```powershell
.\_scripts\install-env.ps1
.\_scripts\build-all.ps1
.\_scripts\gap-audit.ps1
```

Bootstrap / org scripts here still work; new environment scripts must go to `_scripts/`.
